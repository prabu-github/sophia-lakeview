- Model names have the form:
  `<model>__<trait>__<xtrans>__<ytrans>__rep__boa`.

---------------------------------------------------------


- submit: `mkdir sophia-lakeview`. Add `hytraits_container.sif`. 

---------------------------------------------------------

- staging: `mkdir /staging/pravindran/sophia-lakeview` or clean it up

---------------------------------------------------------


- local: in `utils.py`, change default arg for project name to `sophia-lakeview`
  
- local: `cd projworks/sophia-lakeview`

- local: `python model.py --make_compatible`. This creates `io_/compatible`.

- local: `python chtc_create_submits.py` (change args as needed) - generates submit and parameter files for CHTC.

- local: `cd projworks`

- local: `tar -czf forchtc/hytraits.tar.gz --exclude hytraits/useful --exclude hytraits/testutils --exclude hytraits/nbs --exclude hytraits/.git hytraits/`

- local: DELETE unnecessary from `io_`
  
- local: `tar -czf forchtc/sophia-lakeview.tar.gz --exclude sophia-lakeview/chtc --exclude sophia-lakeview/.git --exclude sophia-lakeview/io_/data sophia-lakeview/`
  
- local: `scp forchtc/hytraits.tar.gz forchtc/sophia-lakeview.tar.gz sophia-lakeview/chtc/gens/* sophia-lakeview/chtc/refs/*.sh pravindran@townsend-ap.chtc.wisc.edu:/home/pravindran/sophia-lakeview`

---------------------------------------------------------

- submit: `condor_submit_dag submit.dag`

---------------------------------------------------------

- local: `cd projworks/sophia-lakeview/io_`

- local: `scp pravindran@transfer.chtc.wisc.edu:/staging/pravindran/sophia-lakeview/*packed.tar.gz .`

- local: `for file in *packed.tar.gz; do tar -xzf "$file"; done` (for `*packed.tar.gz`)

- local: move `*packed.tar.gz` files to a `packed` dir for resuse later and to prevent re-extraction in the next step

- local: `for file in *.tar.gz; do tar -xzf "$file"; done` (for individual `*.tar.gz`s)

- local: `rm -f *.tar.gz`

- local: `python post.py`


##################################################################
# SCRATCH
